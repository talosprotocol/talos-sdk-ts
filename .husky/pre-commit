#!/bin/sh
echo "🔍 Running pre-commit validation..."

# 1. Tests (skip lint if not configured)
echo "Running tests..."
npm test
if [ $? -ne 0 ]; then
    echo "❌ Tests failed"
    exit 1
fi

echo "✅ All pre-commit checks passed"
